[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"
  NEXT_TELEMETRY_DISABLED = "1"

# The Next.js runtime handles the App Router, server components, image
# optimisation and the referral redirect route.
[[plugins]]
  package = "@netlify/plugin-nextjs"
